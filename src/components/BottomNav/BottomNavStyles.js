import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    root: {
      width: '100%',
      position: 'fixed',
      bottom: 0,
      backgroundColor: '#111',
      zIndex: 100
    },
  });